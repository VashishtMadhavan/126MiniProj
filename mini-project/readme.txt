Packages to import:
NLTK
Beautiful Soup 4
HTML5Lib
NetworkX

To run the script, execute the command:

python crawler.py

The results will be a list of tuples which contain the keyword and its “score”, which represents how certain keywords appear over Daily Cal domain. 